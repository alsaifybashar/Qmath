/**
 * MathCASInput — re-exports the new EquationInput system.
 * Kept for backwards compatibility with existing imports.
 */
export { default } from './EquationInput/index';
export type { MathCASInputProps, CASQuestionType } from './EquationInput/index';
